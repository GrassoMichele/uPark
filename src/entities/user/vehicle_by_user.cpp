//leggere dal DB, vogliamo gli ids vehicle per un utente
// SELECT id from vehicles where id_user = ?

// get vehicles by user_id = x

//DataMapper<Vehicle> mapperV;
// std::vector<int> vehicles_by_user;
//std::vector<Vehicles> vehicles =mapperV.Read_all();
// for (Vehicle v : vehicles) {
//      if (v.getIdUser == x)
//            vehicles_by_user.push_back(v.getId())
//  }
// return vechicles_by_user;
